Using the defer in javascript or trypscript in html allow the browser to parse the script files for user it helps prevent from blocking and page rendring of javascript files or typescript

# Type script is stronglly typed languages
 
let album: string | number; // we can assing type as union with | where it can hold both the assigned typed 